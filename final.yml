All APIs created with API Gateway expose HTTPS endpoints only.


RCU calculation

Formula:
Strongly consistent: RCU = ceil(ItemSizeKB / 4) * ReadPerSecond
Eventually consistent: RCU = Strongly consistent * 0.5

Gotcha: GSIs in DynamoDB always use eventual consistency — strongly consistent reads are not supported.
Kinesis data streams - Consumer applications can read multiple times; retention configurable (default 24h, up to 7 days).


Stage Variables (API Gateway)

Live in API Gateway, per stage (dev, test, prod).

Used to customize backend integrations without changing code.

Passed to Lambda via mapping templates.

lambda - Concurrency 0	Reserved concurrency set to 0 disables a function

⚠️ Gotcha – Hot Partitions in DynamoDB:
Even if your CloudWatch metrics show consumed capacity below provisioned limits, you can still get throttling errors due to a hot partition.

✅ Fix it by:

Distributing reads/writes evenly across partition keys
Using exponential backoff on retries
💡 When to use DAX:

For read-heavy workloads where repeated reads on the same data cause latency issues
DAX caches queries in memory, giving microsecond latency
❌ Note: DAX does not fix hot partitions for writes and adds cost.


Merging shards reduces parallelism; splitting shards increases it.

Lambda does not automatically throttle due to too many shards, but account concurrency limits still apply.

Example: 100 shards → at most 100 concurrent Lambda executions, regardless of record volume.


⚡ Gotcha: Route 53 vs CloudFront (Geo Confusion!)
🌍 Route 53 Geolocation = "Which door to enter?"

DNS level → decides which endpoint/server (e.g., US users → US ALB, EU users → EU ALB).
Think: traffic routing before entering the building.
📦 CloudFront + Viewer-Country = "What do you get inside?"

Edge function → inspects user’s country, does redirects (e.g., /us/, /uk/).
Think: same building, but different rooms inside.
💡 Memory Trick:
👉 Route 53 = Where to go
👉 CloudFront = What to show

⚠️ Common exam trap: If the scenario is about redirects or personalized content → CloudFront Functions, NOT Route 53.

⚡ Exam Tips
Lambda / Serverless: Blue/Green or Canary preferred for production.
EC2 / ECS: Rolling or Immutable recommended for zero-downtime.
SAM: Typically uses in-place or canary via CodeDeploy.
Always know risk vs downtime trade-off for each strategy.


Memory & CPU: Memory (128 MB – 10,240 MB); CPU scales with memory


lambda Deployment Package
Type	Max Size	Notes
Zip file (direct upload)	50 MB	Includes your code + dependencies
Zip file (via S3)	250 MB	Upload larger code via S3
Container image	10 GB	Supports heavy dependencies, custom runtimes
Handler file must be at root of zip (unless using subdirectories + proper path)
Layers: Shared libraries or code; max 5 layers, total 250 MB


lambda Timeout: 1 sec – 15 min (900 sec max)

lambda Event Sources
Synchronous: API Gateway, SDK calls, CloudWatch Logs → caller waits
Asynchronous: S3, SNS, EventBridge → Lambda invoked in background
Polling: SQS, DynamoDB Streams → Lambda polls events


Lambda Authorizer (API Gateway):
Types: Token (header/query) or Request (any request data)

Cloud formation **StackSet = one template, many accounts/regions at once**  


# Amazon S3  
- Stores deploy artifacts (Lambda zips, CFN templates)  
- CodePipeline artifact store, supports versioning  
✅ Gotcha: SAM/CFN need S3 upload  
💡 Hook: "S3 = Deploy bucket"  


# AWS Amplify  
Full-stack web/mobile deploy; Git branch = env.  
✅ Gotcha: Branch mapping is exam-tested.  

# AWS Copilot  
CLI for ECS deploy; makes envs, services, pipelines.  
✅ Gotcha: ECS-only (not EKS).  


Elastic BeanStalk- Gotcha: For multi-container Docker, Dockerrun.aws.json must be at root, not inside .ebextensions.


Elastic Beanstalk .ebextensions Use Case
Purpose: Automate environment configuration during deployment.
Example Use Case:
Install additional software or packages on EC2 instances.
Create custom directories or config files.
Set environment variables or run commands at deployment.


5️⃣ Execution Context
The sandbox environment for Lambda code.
Includes: runtime, /tmp storage (512 MB), env vars.
Warm start: context reused → faster execution.
Cold start: new context created → slower first run.
✅ Gotcha: /tmp persists only for warm starts, not cold.


Gotcha: Always use IAM roles + trust policies, not IAM users/keys, for cross-account setups.

Automatic Key Rotation:
AWS managed keys → rotated every 3 years.
Customer managed keys → optional yearly rotation.

 Hotlinking Prevention
Hotlinking: Other sites link to your S3 files → you pay bandwidth.
Fix: Use CloudFront + Signed URLs/Cookies.
Gotcha:
S3 Pre-signed URL → object-level temp access.
CloudFront Signed URL/Cookie → best for hotlinking.


Cross-account access: trust policy must allow external account’s IAM principal.

Cross-account access (AWS → AWS): Use IAM Roles + Trust Policies.

External access (on-prem / local machine / external apps): Use long-term AWS access keys (stored in ~/.aws/credentials or env vars).

⚡ Gotcha: For on-prem/external, if possible use federation + AssumeRoleWithSAML/WebIdentity instead of static keys (best practice, avoids hardcoding).

Q: When should you use Lambda Authorizer?
A: When you need custom auth logic (e.g., non-AWS identity provider, special token validation).

⚡ Gotcha
lambda authorizer is Not for “authorizing Lambda directly.”
It’s the gatekeeper at API Gateway.
Response must return an IAM policy (Allow/Deny).


Feature	Cognito Sync (Legacy)	AppSync
Purpose	Sync user data/preferences across devices	Build GraphQL APIs for apps
What it syncs	Key-value pairs (like settings, game scores)	Any app data from DynamoDB, Lambda, RDS, etc.
Scope	Per-user data	App-wide data/API layer
Backend	Uses Cognito Identity + datasets	Managed GraphQL service
Status	Legacy / deprecated → replaced by AppSync	Actively supported, modern choice

X-Ray Enablement Quick Revision
Service	How to Enable X-Ray	Notes
Elastic Beanstalk	.ebextensions/debugging.config → set XRayEnabled: true	Starts X-Ray daemon automatically
Lambda	Enable in function config	Auto traces requests (no daemon req.)
EC2 / ECS / On-prem	Install & run X-Ray daemon + instrument SDK. Use a user data script to install the X-Ray daemon.	Manual setup & instrumentation needed Fargate (awsvpc) → no port mapping; EC2/bridge → UDP
2000



⏰ All DynamoDB Streams stream records last ONLY 24 HOURS!

You can retrieve changes for the last 24 hours, but anything older is gone forever.
Cannot extend retention — no exceptions!
💡 Memory: “If it’s older than 24h, it’s dust!”


Cloudwatch on EC2- “No agent = basic EC2 metrics; agent = logs & custom metrics in CloudWatch.”


⚠️ Gotcha: CloudWatch Monitoring
Take note: CloudWatch does NOT monitor memory, swap, or disk space utilization of your instances by default.
If you need these metrics, you must install the CloudWatch agent on your EC2 instances.

By default, CloudWatch only monitors:

CPU utilization
Network in/out
Disk read/write operations (I/O only, not disk space)
Status checks (system and instance)
VPC Flow Logs – Quick Notes
Purpose: Capture information about IP traffic going in and out of network interfaces in a VPC, subnet, or ENI.


Asynchronous (Event) 🚀: Caller doesn’t wait, Lambda retries on failure. ---ASYNC IS ALSO CALLED AS **EVENT **

 Simple Rules
Want HTTPS for your users? → Focus on CloudFront Viewer Protocol Policy.
Want HTTPS between CloudFront and ALB? → ALB needs a trusted certificate.
Never use self-signed certs in production (browsers won’t trust it).
ALB does not come with a default cert — you must attach one manually.
S3 / storing certs → irrelevant for ALB or CloudFront HTTPS directly.

 Memory Hook
“Viewer HTTPS → CloudFront policy; CloudFront→ALB HTTPS → ACM or CA cert; no self-signed, no defaults.”

ElastiCache: Memcached vs Redis
Feature	Memcached ✅	Redis ❌
Threading / CPU usage	Multithreaded, uses multiple cores	Mostly single-threaded
Scaling	Easy to scale out/in	Requires sharding/replication
Use case	Simple key/value cache	Advanced features, persistence
Key gotcha	Best for multithreaded large nodes	Not ideal for multithreaded nodes
Memory Hook: Memcached = simple, multithreaded, scalable; Redis = rich features but single-threaded.


"X_AMZN_TRACE_ID = trace info, AWS_XRAY_CONTEXT_MISSING = how to handle missing trace context."

Gotcha: Route 53 geolocation routing ≠ content personalization.

It only directs traffic to different resources by region (performance/regulatory).
To serve region-specific pages, use CloudFront-Viewer-Country with edge or origin logic.

xray -- Gotcha:

Use subsegments for internal work.

Use remote subsegments + namespace when calling AWS services or external APIs.




